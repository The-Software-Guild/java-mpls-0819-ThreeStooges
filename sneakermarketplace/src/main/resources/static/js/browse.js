function toggleBrand(brandName) {
    // add or remove brand from stored search query
    
    if(params.brand == brandName) {
        delete params.brand
    }
}

function toggleColor(colorName) {
    
}
